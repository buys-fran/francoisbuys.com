<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@300;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="{{ '/styles.css' | url }}" />
  </head>
  <body class="flex font-light bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-200">
    <aside class="max-h-screen h-screen w-1/3 pt-4 p-8 border-r border-gray-300 dark:border-gray-900">
      {% include "partials/sidebar.njk" %}
    </aside>

    <main class="flex flex-col max-h-screen h-screen w-2/3 overflow-scroll">
      <section class="fixed w-2/3">
        {% include "partials/navigation.njk" %}
     </section>

     <div class="mt-24 px-10 flex-grow">
       {{ content | safe }}
      </div>
    </main>

    {% set js %}
      {% include "js/theme.js" %}
    {% endset %}
    <script>{{ js | jsmin | safe }}</script>

  </body>
</html>
